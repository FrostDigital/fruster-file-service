{
    "id": "DeleteFileRequest",
    "description": "Request object for delete a file from s3 bucket",
    "additionalProperties": false,
    "properties": {
        "url": {
            "description": "The url to the file.",
            "type": "string",
            "format": "uri"
        }
    }
}